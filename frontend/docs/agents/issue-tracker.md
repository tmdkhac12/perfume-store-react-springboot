# Issue tracker

Issues for this repo are tracked in GitHub Issues.

## Workflow

- **Create**: Use `gh issue create`.
- **List**: Use `gh issue list`.
- **View**: Use `gh issue view <number>`.
- **Labels**: Use the vocabulary defined in `docs/agents/triage-labels.md`.
